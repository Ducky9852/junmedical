# 영업도우미 봇 인수인계서

> **대상:** 안티그래비티 인수 담당자  
> **작성일:** 2026-08-25  
> **버전:** v1.3 (DEMO DB 이중 저장, 영업실패 자동 감지, 검토완료 명령 포함)

---

## 목차

1. [시스템 개요](#1-시스템-개요)
2. [전체 아키텍처](#2-전체-아키텍처)
3. [서버 정보 및 접속 방법](#3-서버-정보-및-접속-방법)
4. [GitHub 저장소](#4-github-저장소)
5. [Slack 봇 설정](#5-slack-봇-설정)
6. [환경변수 전체 목록](#6-환경변수-전체-목록)
7. [Notion 데이터베이스 구조](#7-notion-데이터베이스-구조)
8. [Google Sheets 연동](#8-google-sheets-연동)
9. [Gemini AI 설정](#9-gemini-ai-설정)
10. [봇 배포 및 업데이트 방법](#10-봇-배포-및-업데이트-방법)
11. [명령어 전체 레퍼런스](#11-명령어-전체-레퍼런스)
12. [메시지 처리 흐름 (상세)](#12-메시지-처리-흐름-상세)
13. [알려진 버그 및 미해결 이슈](#13-알려진-버그-및-미해결-이슈)
14. [트러블슈팅 가이드](#14-트러블슈팅-가이드)

---

## 1. 시스템 개요

Slack `#영업일지` 채널에 영업 담당자가 자유 형식으로 일지를 입력하면, 봇이 자동으로 Gemini AI로 파싱하여 Notion DB에 구조화 저장하는 시스템입니다.

### 핵심 기능

| 기능 | 설명 |
|------|------|
| 영업일지 자동 저장 | 자유 문장 → Gemini AI 파싱 → 노션 영업관리 DB 저장 |
| A/S 자동 등록 | `A/S·클레임` 액션 감지 시 A/S 관리 DB에 자동 등록 |
| 샘플/데모 이중 저장 | 영업일지 + DEMO 진행중 DB 동시 저장 |
| 영업 기회 추적 | 거래처×제품 단위 칸반 자동 업데이트 |
| 관리 명령어 | 거래처 추가, 약칭 관리, A/S 상태 변경, 데모 회수 등 |
| 병원 현황 조회 | `현황 병원명` → 영업·A/S·DEMO 통합 현황 응답 |

### 담당자 (Slack ID)

| 이름 | Slack User ID |
|------|--------------|
| 이우식 | `U08RXTVB9N0` |
| 이상미 | `U091XTW8AR0` |
| 이은필 | `U0BFNKE5FS8` |
| 이재덕 | `U0BJENWEL7K` |
| 최진웅 | `U0BGDBLJC6L` |
| 원유훈 | `U091YDB52MS` |

> ⚠️ **안티그래비티 직원 추가 시:** `sales_log.py` 상단 `AUTHOR_MAP` 딕셔너리에 `"슬랙UID": "이름"` 형식으로 추가해야 합니다. Slack UID는 Slack에서 해당 프로필 → `...` → `Copy member ID`로 확인합니다.

---

## 2. 전체 아키텍처

```
[Slack 채널]
  #영업일지      ──→ [Slack Events API]
  #샘플데모채널  ──→ [Slack Events API]
                          │
                          ▼
              [Ubuntu 서버 64.110.106.131]
                   salesbot.service
                   Python 3.x
                          │
              ┌───────────┼───────────┐
              │           │           │
              ▼           ▼           ▼
          [Gemini AI]  [Notion API]  [Google Sheets]
       gemini-3.5-flash  REST v1   (품목 마스터)
              │           │
              └─────┬─────┘
                    ▼
             [Notion DBs]
        ┌────────────────────┐
        │ 영업관리 DB        │
        │ 거래처 관리 DB     │
        │ 영업 제품 DB       │
        │ A/S 관리 DB        │
        │ DEMO 진행중 DB     │
        │ 영업 기회 DB       │
        └────────────────────┘
```

---

## 3. 서버 정보 및 접속 방법

| 항목 | 값 |
|------|---|
| 서버 IP | `64.110.106.131` |
| 접속 계정 | `ubuntu` |
| SSH 키 파일 | `ssh-key-2026-07-06.key` |
| 봇 설치 경로 | `/home/ubuntu/bot/` |
| systemd 서비스명 | `salesbot` |

### SSH 접속

```bash
ssh -i ssh-key-2026-07-06.key ubuntu@64.110.106.131
```

### 봇 서비스 관리 명령어

```bash
# 상태 확인
sudo systemctl status salesbot

# 재시작
sudo systemctl restart salesbot

# 로그 실시간 확인
sudo journalctl -u salesbot -f

# 최근 100줄 로그
sudo journalctl -u salesbot -n 100

# 서비스 자동시작 설정
sudo systemctl enable salesbot
```

### 서비스 파일 위치

```
/etc/systemd/system/salesbot.service
```

### 환경변수 파일 위치

```
/home/ubuntu/bot/salesbot.env   (또는 salesbot.service 내 Environment= 라인)
```

서비스 파일 내 환경변수 수정 후에는 반드시 다음 명령 실행:

```bash
sudo systemctl daemon-reload
sudo systemctl restart salesbot
```

---

## 4. GitHub 저장소

### 4-1. 저장소 구조

```
영업 도우미 프로젝트/
├── bot/
│   ├── sales_log.py          # 핵심 봇 로직 (영업일지·데모·A/S 처리)
│   ├── sheet_sync.py         # Google Sheets 품목 동기화 모듈
│   ├── app.py (또는 main.py) # Slack bolt 앱 진입점
│   ├── token.json            # Slack OAuth 토큰 ← 절대 커밋 금지
│   ├── products.json         # 제품 캐시 ← 커밋 제외
│   └── 신규품목.json          # 신규 품목 캐시 ← 커밋 제외
└── 바로가기/
    └── update_bot.ps1        # 배포 자동화 PowerShell 스크립트
```

### 4-2. .gitignore 필수 항목

GitHub에 올리기 전 반드시 아래 파일들을 `.gitignore`에 추가해야 합니다:

```gitignore
# 민감 파일 - 절대 커밋 금지
token.json
products.json
신규품목.json
*.key
*.pem
salesbot.env
.env

# Python 캐시
__pycache__/
*.pyc
*.pyo

# 가상환경
venv/
.venv/
```

### 4-3. GitHub 저장소 새로 만드는 법 (안티그래비티 기준)

```bash
# 1. 서버에 SSH 접속
ssh -i ssh-key-2026-07-06.key ubuntu@64.110.106.131

# 2. 봇 디렉토리로 이동
cd /home/ubuntu/bot

# 3. git 초기화 (이미 되어 있으면 생략)
git init
git remote add origin https://github.com/[안티그래비티조직]/salesbot.git

# 4. 민감 파일 제외 확인 후 커밋
git status
git add sales_log.py sheet_sync.py app.py
git commit -m "feat: 영업도우미 봇 초기 커밋"
git push -u origin main
```

### 4-4. GitHub Actions CI/CD (선택 - 자동 배포 구성 시)

자동 배포를 원할 경우 `.github/workflows/deploy.yml` 파일을 생성합니다:

```yaml
name: Deploy salesbot

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 64.110.106.131
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/ubuntu/bot
            git pull origin main
            sudo systemctl restart salesbot
```

GitHub 저장소 Settings → Secrets and variables → Actions에서 `SSH_PRIVATE_KEY` 시크릿을 등록해야 합니다.

### 4-5. 현재 배포 방식 (PowerShell 스크립트)

GitHub Actions 대신 Windows에서 실행하는 PowerShell 배포 스크립트(`update_bot.ps1`)를 사용 중입니다.

**`update_bot.ps1` 주요 로직:**

```powershell
# 수정된 파일만 업로드 (MD5 해시 비교)
$skipFiles = @("token.json", "products.json", "신규품목.json")

foreach ($file in $localFiles) {
    if ($skipFiles -contains $file.Name) { continue }  # 민감 파일 건너뜀
    
    # 로컬 MD5와 서버 MD5 비교
    $localMd5  = Get-FileHash $file.FullName -Algorithm MD5
    $serverMd5 = (ssh 서버접속 "md5sum /home/ubuntu/bot/$($file.Name)")
    
    if ($localMd5 -ne $serverMd5) {
        scp -i $sshKey $file.FullName ubuntu@64.110.106.131:/home/ubuntu/bot/
        "업로드: $($file.Name)"
    }
}

# 업로드 후 서비스 재시작
ssh 서버 "sudo systemctl restart salesbot"
```

> ⚠️ PowerShell 스크립트에서 한글이 깨지는 경우 파일 인코딩이 **UTF-8 BOM**이어야 합니다.  
> VSCode에서 파일 우하단 인코딩 클릭 → `UTF-8 with BOM` 선택.

---

## 5. Slack 봇 설정

### 5-1. Slack 앱 생성 (https://api.slack.com/apps)

1. `Create New App` → `From scratch`
2. App Name: `영업도우미` (또는 원하는 이름)
3. Workspace: 안티그래비티 워크스페이스 선택

### 5-2. OAuth 권한 설정 (OAuth & Permissions)

**Bot Token Scopes** 탭에서 아래 권한을 모두 추가해야 합니다:

| Scope | 용도 |
|-------|------|
| `channels:history` | 채널 메시지 읽기 |
| `channels:read` | 채널 정보 읽기 |
| `chat:write` | 메시지 전송 (스레드 댓글) |
| `im:write` | DM 전송 (관리자 오류 알림) |
| `users:read` | 사용자 정보 읽기 |
| `groups:history` | 비공개 채널 메시지 읽기 (필요 시) |

권한 추가 후 **Install to Workspace** 버튼 클릭 → Bot User OAuth Token 복사 (`xoxb-...` 형식).

### 5-3. Event Subscriptions 설정

Slack 앱 관리 페이지에서 **Event Subscriptions** → Enable Events ON:

**Request URL:** `https://[서버주소 또는 ngrok]/slack/events`

> ⚠️ 서버가 공인 IP를 갖고 있으면 `http://64.110.106.131:[포트]/slack/events` 형식으로 입력.  
> ngrok 사용 시 매번 URL이 바뀌므로 고정 도메인 서비스 또는 공인 IP 직접 사용 권장.

**Subscribe to bot events** 에 추가해야 할 이벤트:

| Event | 용도 |
|-------|------|
| `message.channels` | 공개 채널 메시지 수신 |
| `message.groups` | 비공개 채널 메시지 수신 (필요 시) |
| `app_mention` | 봇 멘션 이벤트 (선택) |

### 5-4. 채널 ID 확인 방법

채널 ID는 `C` 로 시작하는 11자리 코드입니다.

**방법 1 - Slack 앱에서:**
1. Slack에서 해당 채널 우클릭
2. `채널 세부 정보 보기` → 맨 아래 채널 ID 복사

**방법 2 - URL에서:**
```
https://app.slack.com/client/T0XXXXXXX/C0XXXXXXX
                                       ^^^^^^^^^^^
                                       이 부분이 채널 ID
```

**방법 3 - Slack API:**
```bash
curl -H "Authorization: Bearer xoxb-..." \
  https://slack.com/api/conversations.list | python3 -c "
import json, sys
data = json.load(sys.stdin)
for c in data['channels']:
    print(c['id'], c['name'])
"
```

### 5-5. 사용자 ID 확인 방법

User ID는 `U` 로 시작하는 11자리 코드입니다.

**방법 1 - Slack 프로필에서:**
1. Slack에서 해당 사람 프로필 클릭
2. `...` (더 보기) → `Copy Member ID`

**방법 2 - Slack API:**
```bash
curl -H "Authorization: Bearer xoxb-..." \
  "https://slack.com/api/users.list" | python3 -c "
import json, sys
data = json.load(sys.stdin)
for u in data['members']:
    if not u.get('is_bot'):
        print(u['id'], u['real_name'])
"
```

### 5-6. 봇 채널 초대

봇을 채널에 초대하지 않으면 메시지를 받을 수 없습니다:

```
/invite @영업도우미
```

`#영업일지` 채널과 `#샘플데모채널`(DEMO_CHANNEL) 모두 초대해야 합니다.

### 5-7. AUTHOR_MAP 업데이트

`sales_log.py` 상단 `AUTHOR_MAP` 딕셔너리에 안티그래비티 직원 추가:

```python
AUTHOR_MAP = {
    # 기존 직원 (필요 시 삭제 또는 유지)
    "U08RXTVB9N0": "이우식",
    "U091XTW8AR0": "이상미",
    ...
    
    # 안티그래비티 신규 직원 추가
    "U0XXXXXXXXX": "홍길동",   # ← Slack User ID 확인 후 추가
    "U0YYYYYYYYY": "김철수",
}
```

### 5-8. 관리자 DM 설정 (ADMIN_SLACK_ID)

오류 발생 시 관리자에게 DM을 보냅니다.  
`ADMIN_SLACK_ID` 환경변수에 관리자의 Slack User ID를 설정합니다.

```
# 예시
ADMIN_SLACK_ID=U08RXTVB9N0
```

### 5-9. Slack 봇 토큰 저장

서버의 `/home/ubuntu/bot/token.json`에 다음 형식으로 저장합니다:

```json
{
  "bot_token": "xoxb-your-bot-token-here",
  "signing_secret": "your-signing-secret-here"
}
```

또는 환경변수로 직접 설정:

```bash
SLACK_BOT_TOKEN=xoxb-...
SLACK_SIGNING_SECRET=...
```

> **Signing Secret 위치:** Slack 앱 페이지 → Basic Information → App Credentials → Signing Secret

### 5-10. 채널 ID 환경변수 매핑

```bash
# 영업일지 채널 (메인 채널)
SALES_LOG_CHANNEL=C0XXXXXXXXX   # #영업일지 채널 ID

# 샘플/데모 전용 채널 (선택 - 없으면 영업일지에서도 처리)
DEMO_CHANNEL=C0YYYYYYYYY        # #샘플데모 채널 ID
```

---

## 6. 환경변수 전체 목록

서버의 `salesbot.service` 파일(`/etc/systemd/system/salesbot.service`) 안에 모두 설정합니다.

```ini
[Service]
User=ubuntu
WorkingDirectory=/home/ubuntu/bot
ExecStart=/usr/bin/python3 /home/ubuntu/bot/app.py

# ── Slack ────────────────────────────────────────────────
Environment=SLACK_BOT_TOKEN=xoxb-...
Environment=SLACK_SIGNING_SECRET=...
Environment=SALES_LOG_CHANNEL=C0XXXXXXXXX
Environment=DEMO_CHANNEL=C0YYYYYYYYY
Environment=ADMIN_SLACK_ID=U0XXXXXXXXX

# ── Notion ───────────────────────────────────────────────
Environment=NOTION_TOKEN=secret_...
Environment=NOTION_DB_SALES=b04f69a2-ffd0-499e-adac-fd012de4de41
Environment=NOTION_DB_CLIENTS=[거래처 관리 DB ID]
Environment=NOTION_DB_PRODUCTS=3001cfe9-3e35-49e2-83da-0e4ba4ad61bd
Environment=NOTION_DB_AS=[A/S 관리 DB ID]
Environment=NOTION_DB_DEMO=[DEMO 진행중 DB ID]
Environment=NOTION_DB_OPPORTUNITY=5e442046-5590-40c4-93f3-4c41f417ae04

# ── Gemini AI ────────────────────────────────────────────
Environment=GEMINI_API_KEY=AIza...
Environment=SALES_GEMINI_MODEL=gemini-3.5-flash

# ── Google Sheets ────────────────────────────────────────
Environment=GSHEET_ID=1ABC...spreadsheet_id...
Environment=GOOGLE_SA_JSON=/home/ubuntu/bot/google-sa.json
```

### Notion DB ID 확인 방법

1. 노션에서 해당 DB 열기
2. URL 확인: `https://www.notion.so/[워크스페이스]/[DB_ID]?v=...`
3. `?v=` 앞의 32자리 UUID가 DB ID

또는 노션 DB 우상단 `...` → `Copy link` → URL에서 추출.

### Notion Integration Token 발급

1. `https://www.notion.so/my-integrations` 접속
2. `+ New integration` 클릭
3. 이름: `영업도우미봇`
4. `Submit` → `Internal Integration Token` 복사 (`secret_...` 형식)
5. 각 노션 DB에서 우상단 `...` → `Connections` → 생성한 Integration 추가

---

## 7. Notion 데이터베이스 구조

### 7-1. 영업관리 DB (NOTION_DB_SALES)

메인 영업일지 저장 DB

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `영업 내용` | Title | 제목 (자동 생성: 병원명 + 제품명 + 행동) |
| `업무 내용` | Text | AI 요약 내용 + 다음 할 일 |
| `거래처` | Relation | 거래처 관리 DB 연결 |
| `영업 제품` | Relation | 영업 제품 DB 연결 |
| `담당자` | Select | 영업 담당자 이름 |
| `Action_Type` | Select | 신규접촉/제품설명·소개/샘플·데모/견적제출/납품·설치/A/S·클레임/관계관리/기타 |
| `상태` | Status | 영업시도/영업중/영업완료/영업실패 |
| `검토상태` | Select | 완료/신규확인필요 |
| `신규or기존` | Select | 신규 병원/기존 병원 |
| `영업일` | Date | 영업 날짜 (Slack 메시지 타임스탬프 기준) |
| `지역그룹` | Select | 천안아산/대전논산/서산당진/서울경기/평택/세종충북 |
| `거래처 담당자` | Multi-select | 거래처 측 담당자 이름(들) |

> ⚠️ `영업일` 필드의 Notion 내부 쿼리 컬럼명은 `"date:영업일:start"` 입니다 (API 직접 쿼리 시 주의).

### 7-2. 거래처 관리 DB (NOTION_DB_CLIENTS)

병원·거래처 마스터 데이터

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `이름` | Title | 거래처 정식명칭 |
| `약칭` | Text | 쉼표(`,`) 구분으로 여러 약칭 등록 가능 |

> **약칭 매칭 로직:** 봇은 메시지의 병원명을 정식명 또는 약칭으로 매칭합니다.  
> 예) `혜성산부` 입력 → `약칭: "혜성산부"` 가 등록된 거래처로 매칭.

### 7-3. 영업 제품 DB (NOTION_DB_PRODUCTS)

제품 마스터 (Google Sheets에서 동기화)

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `이름` | Title | 제품 정식명 |

> 제품 검색어(키워드)는 Google Sheets에서 관리됩니다. 노션 영업 제품 DB는 relation 연결 대상으로만 사용.

### 7-4. A/S 관리 DB (NOTION_DB_AS)

A/S 접수 및 처리 현황

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `제목` | Title | A/S 항목 제목 |
| `처리 상태` | Select | 접수됨/업체 발송됨/자체 수리중/수리 완료/병원 반납 완료 |
| `태그` | Select | 좌욕기 A/S/복강경 A/S/불량 교체건 |
| `A/S 요청병원` | Relation | 거래처 관리 DB 연결 |
| `비고` | Text | 상세 내용 |
| `A/S 발송일` | Date | 업체 발송 날짜 (AS발송 명령 실행 시 자동 기록) |

> ⚠️ **알려진 버그:** `현황` 명령 A/S 날짜 표시 시 `"접수일"` 필드를 읽으나  
> 실제 필드명은 `"A/S 발송일"` → 날짜가 공백으로 표시됨.  
> **수정 위치:** `sales_log.py` `build_status_reply()` 함수 약 713번째 줄:
> ```python
> # 수정 전 (버그)
> date = _date_of(p, "접수일")
> # 수정 후
> date = _date_of(p, "A/S 발송일")
> ```

### 7-5. DEMO 진행중 DB (NOTION_DB_DEMO)

샘플·데모 진행 현황

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `제목` | Title | 자동 생성: `샘플-병원명-제품명` 또는 `DEMO-병원명_제품명` |
| `진행 상태` | Select | 샘플 발송/데모 설치 중/회수 완료/결과 확인 완료 |
| `유형` | Select | 샘플/데모 |
| `DEMO 병원` | Relation | 거래처 관리 DB 연결 |
| `모델명` | Text | 제품 모델명 또는 코드 |
| `시리얼번호` | Text | 시리얼 번호 (S/N) |
| `태그` | Select | 복강경 제품/Flexible Cysto/보비 |
| `날짜` | Date | 샘플/데모 출고 날짜 |
| `회수 예정일` | Date | 데모만 해당 (샘플은 없음), 기본값 = 출고일 +7일 |
| `회수일` | Date | 실제 회수 날짜 |
| `결과` | Select | 구매로 이어짐/미구매/팔로업 필요/확인 중 |
| `담당자` | Select | 담당 직원 이름 |
| `비고` | Text | 담당자 정보 + 기타 메모 |

> **샘플 vs 데모 차이:**  
> - 샘플: `진행 상태 = 샘플 발송`, 회수 예정일 없음 → `샘플결과` 명령으로 결과 입력  
> - 데모: `진행 상태 = 데모 설치 중`, 회수 예정일 = +7일 → `데모회수` 명령으로 회수 처리

### 7-6. 영업 기회 DB (NOTION_DB_OPPORTUNITY)

거래처 × 제품 단위 칸반 보드 (자동 관리)

기본 DB ID: `5e442046-5590-40c4-93f3-4c41f417ae04`

| 필드명 | 타입 | 설명 |
|--------|------|------|
| `제목` | Title | 자동 생성: `병원명 × 제품명` |
| `현재 상태` | Select | 탐색중/키맨확인/샘플진행/협상중/계약완료/영업중단 |
| `거래처` | Relation | 거래처 관리 DB 연결 |
| `영업 제품` | Relation | 영업 제품 DB 연결 |
| `담당자` | Select | 담당 직원 |
| `영업관리` | Relation | 영업관리 DB 연결 (복수 링크 가능) |

**Action_Type → 영업 기회 상태 자동 매핑:**

| Action_Type | 영업 기회 상태 |
|------------|--------------|
| 신규접촉 | 탐색중 |
| 제품설명·소개 | 키맨확인 |
| 샘플·데모 | 샘플진행 |
| 견적제출 | 협상중 |
| 납품·설치 | 계약완료 |
| A/S·클레임 | 계약완료 |
| 관계관리, 기타 | 탐색중 |

### 7-7. 노션 링크드뷰 구성 (거래처 관리 DB 기준)

거래처 관리 DB에서 `+ Add a view` → `Linked view of database` 로 아래 필터 뷰를 추가하면 병원별 통합 현황을 노션에서 볼 수 있습니다:

| 뷰 이름 | 원본 DB | 필터 |
|---------|---------|------|
| 영업 이력 | 영업관리 DB | 거래처 = [현재 페이지] |
| A/S 현황 | A/S 관리 DB | A/S 요청병원 = [현재 페이지] |
| 데모/샘플 현황 | DEMO 진행중 DB | DEMO 병원 = [현재 페이지] |
| 영업 기회 | 영업 기회 DB | 거래처 = [현재 페이지] |

---

## 8. Google Sheets 연동

### 8-1. 용도

제품 마스터 데이터(제품명 + 검색어/키워드)를 Google Sheets에서 관리합니다.  
봇은 `sheet_sync.py`를 통해 Sheets에서 제품 목록을 읽어와 메시지 파싱에 활용합니다.

### 8-2. Google Sheets 구조

| 열 | 내용 |
|----|------|
| A | 제품명 (정식) |
| B | 검색어1 |
| C | 검색어2 |
| ... | 추가 검색어 |

### 8-3. Google Service Account 설정

1. Google Cloud Console에서 프로젝트 생성
2. `APIs & Services` → `Google Sheets API` 활성화
3. `IAM & Admin` → `Service Accounts` → 새 서비스 계정 생성
4. 키 생성 → JSON 다운로드 → 서버에 업로드 (`/home/ubuntu/bot/google-sa.json`)
5. Google Sheets에서 해당 서비스 계정 이메일을 편집자로 공유

### 8-4. 환경변수

```bash
GSHEET_ID=[스프레드시트 URL에서 /d/ 뒤 ~까지 부분]
GOOGLE_SA_JSON=/home/ubuntu/bot/google-sa.json
```

---

## 9. Gemini AI 설정

### 9-1. API 키 발급

1. `https://aistudio.google.com/app/apikey` 접속
2. `Create API Key` 클릭
3. 발급된 키를 `GEMINI_API_KEY` 환경변수에 설정

### 9-2. 사용 모델

```bash
SALES_GEMINI_MODEL=gemini-3.5-flash   # 기본값, 비용 절감형
# 고성능이 필요하면:
# SALES_GEMINI_MODEL=gemini-1.5-pro
```

### 9-3. 영업일지 파싱 규칙 (Gemini 프롬프트 요약)

봇은 다음 필드를 추출합니다:

| 필드 | 설명 |
|------|------|
| `hospital` | 병원명 (거래처 목록 기준 정식명 변환) |
| `contact` | 거래처 담당자 이름 |
| `item` | 제품명 목록 |
| `action_type` | 영업 행동 분류 |
| `summary` | 2문장 요약 |
| `next_action` | 다음 할 일 |
| `sales_status` | 영업시도/영업실패 자동 판단 |

**영업실패 자동 감지 키워드 예시:**  
"사용이 어렵다", "도입 안 하겠다", "사용 불편", "진입 불가", "타사 제품만 쓰겠다" 등

---

## 10. 봇 배포 및 업데이트 방법

### 10-1. PowerShell 스크립트로 배포 (Windows)

`update_bot.ps1` 실행:

```powershell
# 위치: C:\Users\[사용자]\Claude\Projects\영업 도우미 프로젝트\바로가기\update_bot.ps1
# 우클릭 → PowerShell로 실행
```

스크립트가 자동으로:
1. 로컬 파일과 서버 파일의 MD5 해시를 비교
2. 변경된 파일만 SCP로 업로드
3. `token.json`, `products.json`, `신규품목.json` 은 건너뜀
4. 업로드 후 `sudo systemctl restart salesbot` 실행

### 10-2. 수동 배포 (서버 직접 수정)

```bash
# 서버 접속
ssh -i ssh-key-2026-07-06.key ubuntu@64.110.106.131

# 파일 편집
nano /home/ubuntu/bot/sales_log.py

# 서비스 재시작
sudo systemctl restart salesbot

# 로그 확인
sudo journalctl -u salesbot -f
```

### 10-3. 최초 서버 설정 (신규 서버 구성 시)

```bash
# Python 패키지 설치
pip3 install slack-bolt google-generativeai google-auth gspread

# 봇 디렉토리 생성
mkdir -p /home/ubuntu/bot

# 파일 업로드 (로컬에서)
scp -i ssh-key-2026-07-06.key sales_log.py ubuntu@64.110.106.131:/home/ubuntu/bot/
scp -i ssh-key-2026-07-06.key sheet_sync.py ubuntu@64.110.106.131:/home/ubuntu/bot/
scp -i ssh-key-2026-07-06.key app.py ubuntu@64.110.106.131:/home/ubuntu/bot/
scp -i ssh-key-2026-07-06.key google-sa.json ubuntu@64.110.106.131:/home/ubuntu/bot/

# systemd 서비스 등록
sudo nano /etc/systemd/system/salesbot.service
sudo systemctl daemon-reload
sudo systemctl enable salesbot
sudo systemctl start salesbot
```

---

## 11. 명령어 전체 레퍼런스

`#영업일지` 채널에서 아래 명령어를 입력하면 봇이 처리합니다.

### 거래처·약칭 관리

```
거래처추가 {정식병원명}
  → 노션 거래처 관리 DB에 신규 등록
  예) 거래처추가 안티그래비티병원

약칭추가 {정식명 또는 코드} + {약칭}
  → 거래처 약칭 또는 제품 검색어 추가
  예) 약칭추가 혜성산부인과병원 + 혜성산부
  예) 약칭추가 DVT SLEEVE + DVT슬리브
```

### A/S 상태 관리

```
AS발송 {병원명}
  → A/S 상태: 업체 발송됨 / A/S 발송일: 오늘

AS자체수리 {병원명}
  → A/S 상태: 자체 수리중

AS수리완료 {병원명}
  → A/S 상태: 수리 완료

AS반납 {병원명}
  → A/S 상태: 병원 반납 완료
```

> 병원명은 노션 거래처의 정식명 또는 등록된 약칭 모두 가능합니다.

### 데모·샘플 관리

```
데모등록 {병원명} [모델명] [시리얼번호] [YYYY-MM-DD]
  → DEMO 진행중 DB에 수동 등록
  예) 데모등록 충남대학교병원 SJ-ELS102-1 SN12345 2026-09-01

데모회수 {병원명} [YYYY-MM-DD] [결과]
  → 데모 회수 완료 처리
  결과 옵션: 구매로 이어짐 / 미구매 / 팔로업 필요 / 확인 중
  예) 데모회수 충남대학교병원 2026-09-05 구매로 이어짐

샘플결과 {병원명} {결과}
  → 샘플 결과 입력 (진행 상태: 결과 확인 완료)
  예) 샘플결과 청주하나병원 미구매
```

### 검토 및 현황 조회

```
검토완료 {병원명}
  → 해당 거래처의 '신규확인필요' 항목 전체 → '완료' 일괄 변경

현황 {병원명}
  → 해당 거래처의 영업/A/S/DEMO 통합 현황 조회
  예) 현황 충남대학교병원
```

### 자동 처리 (명령 불필요)

영업일지 채널에 일반 영업 메시지를 입력하면 자동으로:

1. Gemini AI가 파싱
2. 영업관리 DB 저장
3. `A/S·클레임` → A/S 관리 DB 자동 등록
4. `샘플·데모` → DEMO 진행중 DB 추가 저장
5. 영업 기회 DB 상태 자동 업데이트

---

## 12. 메시지 처리 흐름 (상세)

### 12-1. 일반 영업일지 처리

```
메시지 입력
    │
    ▼
[관리명령 확인] ──── 명령어 맞으면 → 처리 후 종료
    │ (일반 메시지)
    ▼
[전용 데모채널?] ─── YES → handle_demo_event() → 종료
    │ (NO: 영업일지 채널)
    ▼
[Gemini 추출]
    │
    ▼
[거래처 매칭] → 못 찾으면: is_new_h=True (경고 표시)
    │
    ▼
[제품 매칭] → 못 찾으면: is_new_i=True (경고 표시)
    │
    ▼
[Action_Type == "A/S·클레임"?]
    ├── YES → A/S DB 저장 → 영업관리 DB 건너뜀 → 종료
    └── NO → 영업관리 DB 저장
                │
                ▼
         [영업 기회 DB 동기화]
                │
                ▼
         [Action_Type == "샘플·데모"?]
                ├── YES → DEMO DB 추가 저장
                └── NO → 종료
                │
                ▼
         [Slack 스레드에 결과 댓글]
```

### 12-2. 검토상태 결정 로직

```python
review = "신규확인필요" if (
    is_new_h        # 거래처 DB에 없는 새 병원
    or not cmatch   # 거래처 매칭 실패
    or is_new_i     # 제품 DB에 없는 새 제품
    or new_items    # 매칭 안 된 품목 있음
) else "완료"
```

→ `신규확인필요` 상태 항목은 관리자가 노션에서 확인 후 `검토완료 {병원명}` 명령으로 일괄 완료 처리.

### 12-3. 지역그룹 자동 추론

병원명 앞자리를 기준으로 자동 분류:

| 지역그룹 | 해당 지역 |
|---------|---------|
| 천안아산 | 천안, 아산 |
| 대전논산 | 대전, 논산, 공주, 계룡 |
| 서산당진 | 서산, 당진, 홍성, 보령, 예산 |
| 세종충북 | 청주, 충주, 세종, 옥천, 보은, 영동, 진천, 음성 등 |
| 평택 | 평택 |
| 서울경기 | 나머지 또는 하드코딩 목록 |

특정 병원은 `_REGION_HOSPITALS` 딕셔너리에 하드코딩되어 있습니다. 새 병원이 잘못 분류되면 해당 딕셔너리에 추가하면 됩니다.

---

## 13. 알려진 버그 및 미해결 이슈

### 버그 1: `현황` 명령 A/S 날짜 공백 표시 (미수정)

- **위치:** `sales_log.py` → `build_status_reply()` 함수 (약 713번째 줄)
- **원인:** `_date_of(p, "접수일")` 로 읽으나 실제 A/S 관리 DB에는 `"A/S 발송일"` 필드만 존재
- **수정:**
  ```python
  # 수정 전
  date = _date_of(p, "접수일")
  # 수정 후
  date = _date_of(p, "A/S 발송일")
  ```

### 버그 2: `현황` 명령 A/S 건수와 최근 내역 불일치 가능성

현황 조회 시 A/S 완료 건(`병원 반납 완료`)과 미완료 건이 섞여 표시됩니다.  
필요 시 미완료 건만 필터링하는 로직 추가가 필요합니다.

### 미결 사항

| 항목 | 상태 |
|------|------|
| A/S 날짜 버그 수정 | 미완료 |
| 다중 데모 회수 시 최신 건만 처리 | 설계대로 동작 중 (의도된 동작) |
| 영업 기회 DB 계약완료 건 재활성화 로직 | 미구현 |
| 봇 응답 이모지/포맷 커스터마이징 | 추후 개선 |

---

## 14. 트러블슈팅 가이드

### 봇이 메시지에 반응하지 않을 때

```bash
# 1. 서비스 상태 확인
sudo systemctl status salesbot

# 2. 로그 확인
sudo journalctl -u salesbot -n 50

# 3. 서비스가 죽어있으면 재시작
sudo systemctl restart salesbot
```

### Gemini API 오류가 날 때

- `GEMINI_API_KEY` 환경변수 확인
- Google AI Studio에서 API 사용량/할당량 확인
- `SALES_GEMINI_MODEL` 이 유효한 모델명인지 확인

### 거래처 매칭 실패 (`거래처를 찾을 수 없음`)

```
# 해결 방법:
1. 거래처추가 {정확한 병원명}      # 새 거래처 등록
또는
2. 약칭추가 {기존 정식명} + {봇이 인식 못 하는 약칭}
```

### 제품 매칭 실패 (영업 제품 미연결 경고)

```
# 해결 방법:
1. Google Sheets에서 해당 제품에 키워드 추가
또는
2. 약칭추가 {제품명} + {봇이 인식 못 하는 키워드}
```

### Notion API 오류 (`Notion API 4XX`)

- `NOTION_TOKEN` 만료 여부 확인 (만료 없음 - Notion Internal Integration은 만료 없음)
- 해당 DB에 Integration이 연결되어 있는지 확인 (노션 DB → `...` → Connections)
- DB ID가 올바른지 확인

### PowerShell 스크립트에서 한글 깨짐

`update_bot.ps1` 파일 인코딩을 **UTF-8 BOM**으로 저장:
- VSCode: 파일 우하단 인코딩 클릭 → `UTF-8 with BOM`
- 또는 메모장에서 다른 이름으로 저장 → 인코딩 `UTF-8 (BOM 있음)` 선택

### SSH 접속 키 권한 오류

```bash
# 키 파일 권한 설정 (Linux/Mac)
chmod 400 ssh-key-2026-07-06.key

# Windows PowerShell에서는 파일 속성 → 보안 탭에서 권한 제한 필요
```

---

*이 문서는 2026-08-25 기준으로 작성되었습니다. 코드 변경 시 함께 업데이트해 주세요.*
